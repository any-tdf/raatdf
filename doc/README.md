# RAATDF 项目文档网站

这是 RAATDF 企业级管理后台模板的文档网站，使用 React + Vite + Tailwind CSS v4 构建。

## 特性

- 📖 **Markdown 文档渲染** - 使用 marked 库渲染 Markdown 文档
- 🎨 **现代化设计** - 采用 Tailwind CSS v4 和 Typography 插件
- 📱 **响应式布局** - 完美支持各种屏幕尺寸
- 🌓 **深色模式** - 内置深色模式支持（通过 Tailwind）
- 🚀 **高性能** - 快速的开发和构建体验

## 技术栈

- **React** 19.2.0 - UI 框架
- **Vite** 7.2.4 - 构建工具
- **Tailwind CSS** v4 - 样式框架
- **@tailwindcss/typography** - 文本排版插件
- **marked** - Markdown 解析库

## 快速开始

### 安装

```bash
cd doc
bun install
```

### 开发

```bash
bun dev
```

访问 `http://localhost:5173` 查看文档网站（或其他可用端口）。

### 构建

```bash
bun build
```

生成的静态站点位于 `dist` 文件夹。

### 预览

```bash
bun preview
```

预览生产构建的结果。

## 项目结构

```
doc/
├── src/
│   ├── App.jsx           # 单页面应用主组件
│   ├── main.css          # Tailwind 导入和配置
│   ├── main.jsx          # 应用入口
│   └── assets/           # 静态资源
├── md/
│   ├── guide.md          # 开发指南
│   ├── changelog.md      # 更新日志
│   └── faq.md            # 常见问题
├── index.html            # HTML 入口
├── vite.config.js        # Vite 配置
├── biome.json            # Biome 代码质量检查配置
└── package.json          # 项目配置
```

## 文档

项目包含以下三个文档：

1. **开发指南** (`GUIDE.md`) - 详细的项目架构和开发步骤
2. **更新日志** (`CHANGELOG.md`) - 版本更新记录
3. **常见问题** (`FAQ.md`) - 开发过程中的常见问题解答

## 样式配置

### Tailwind CSS v4

项目使用最新的 Tailwind CSS v4，无需单独的 `tailwind.config.js` 文件。样式配置通过 CSS 文件完成：

```css
@import "tailwindcss";
@plugin "@tailwindcss/typography";
```

### Typography 插件

使用 `@tailwindcss/typography` 提供的 `prose` 类来设置 Markdown 内容的排版样式。

## 功能说明

### 自动生成目录

应用自动从 Markdown 文档的标题生成目录，支持快速导航。

### 侧边栏导航

左侧侧边栏显示文档列表，点击可切换文档，同时显示当前文档的目录。

### 深色模式

通过 Tailwind 的 `dark:` 前缀和 `dark:prose-invert` 类实现深色模式支持。

## 部署

### 静态托管

生产构建后的 `dist` 文件夹可以直接部署到任何静态主机服务：

```bash
# 构建
bun build

# 部署 dist 文件夹到你的服务器
```

### Netlify/Vercel

```bash
# 直接连接 Git 仓库，配置 build command: bun build
# 配置 publish directory: dist
```

## 开发提示

- 使用 `bun add` 添加新的依赖
- 修改 `src/App.jsx` 中的 `docs` 对象来添加新文档
- 在 `public/` 文件夹中添加新的 `.md` 文件
- Tailwind 的热更新会自动应用

## 常见问题

**Q: 如何添加新的文档？**

A:
1. 在 `public/` 文件夹中添加新的 `.md` 文件
2. 在 `src/App.jsx` 的 `docs` 对象中添加新条目

```jsx
const docs = {
  guide: { name: '开发指南', file: '/GUIDE.md' },
  changelog: { name: '更新日志', file: '/CHANGELOG.md' },
  faq: { name: '常见问题', file: '/FAQ.md' },
  newDoc: { name: '新文档', file: '/NEW_DOC.md' }, // 添加
}
```

**Q: 如何自定义样式？**

A:
- 修改 `src/App.css` 中的 Tailwind 类
- 使用 `src/index.css` 中的 `@plugin` 指令添加插件
- 对于 Markdown 内容，修改 `App.jsx` 中的 `prose` 类配置

**Q: 如何部署到生产环境？**

A:
```bash
# 1. 构建
bun build

# 2. 部署 dist 文件夹
# 可以使用 Netlify, Vercel, GitHub Pages 等服务
```

## 许可证

MIT

